# ADR 0002：以 Git 发布内容为权威

知识花园的公开内容、笔记数量和字数统计统一以当前 Git `HEAD` 为权威来源，而不直接采用本地工作区中的全部 Markdown。构建阶段仅保留 Git 已追踪的内容，领域卡片的总字数取当前 `HEAD` 正文字符数，今日变化取 Asia/Shanghai 今日零点前最后一次提交与当前 `HEAD` 的净差值。这样本地预览与 GitHub Pages 使用同一发布边界，代价是新建但尚未提交的笔记不会出现在站点中，并且部署工作流必须拉取完整 Git 历史。
