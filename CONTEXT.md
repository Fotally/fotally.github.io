# Aya 的知识花园

这是一个用于整理、连接并呈现个人学习笔记的数字知识花园。站点同时服务于主题探索与持续阅读。

## 页面类型

**知识花园首页**：
用于介绍知识花园并引导访问者探索知识领域的入口页面，强调主题概览与发现。
_避免使用_：文档首页、文章首页

**内容阅读页**：
承载单篇笔记、目录或标签内容的阅读页面，强调连续阅读与内容导航。
_避免使用_：通用文档页面

## 内容层级

**知识领域**：
知识花园中的第一层主题范围，用于聚合若干相关主题与笔记。
_避免使用_：一级分类、栏目

**主题**：
知识领域内可继续包含子主题与笔记的知识范围；主题允许形成多层层级。
_避免使用_：二级分类、固定层级分类

**层级首页**：
知识领域或任意层级主题自身的介绍与导航页面，属于该层级入口，而不是普通笔记。
_避免使用_：目录说明、普通索引笔记

**笔记数量**：
某一知识领域或主题下全部普通笔记的总数，包含更深层主题中的笔记，但不包含各层级首页。
_避免使用_：页面数量、文件数量

**已发布笔记**：
存在于当前 Git `HEAD` 中的 Markdown 笔记，是构建知识花园时唯一可信的内容范围；未追踪的新文件不属于已发布内容。
_避免使用_：工作区笔记、本地笔记

**今日字数变化**：
以 Asia/Shanghai 的自然日为边界，当前 `HEAD` 与今日零点前最后一次提交之间的正文字符净变化；增加为正数，删减为负数。
_避免使用_：今日编辑字数、今日文件字数

**总字数**：
当前 `HEAD` 中某知识领域全部普通笔记的正文字符总数，不包含 Markdown 标记、空白和层级首页。
_避免使用_：文件大小、源码字符数
