# 文档维护规范

## 参考资料

- 编写包含参考资料的内容时，必须使用语义化 Markdown 脚注标识（例如 `[^claude-code-skills]`），由渲染器根据引用首次出现的顺序自动生成连续编号。
- 禁止使用 `[^1]`、`[^2]` 等需要人工维护顺序的固定数字标识，也禁止在正文中手工维护固定编号引用。

## 索引维护

- `content` 是内容根目录。每次新建、更新、移动或删除文章后，必须从文章的直接父目录开始逐层检查并维护 `index.md`，直到 `content/index.md`。
- 每一级 `index.md` 只列出直接子级：目录索引使用 `[[目录名/index|目录名]]`，文章使用 `[[文章名]]`。
- 索引是简洁的领域导览。每个条目除链接外，还应提供一句话简介，说明该领域、主题或文章解决的问题。
- 一级目录表示领域；二级目录表示领域下可继续拆分的内容。若文章直接位于一级领域目录下，表示该主题无需继续拆分为多篇文档。
- 同级内容优先按阅读与认知顺序排列：基础概念在前，机制与实践居中，进阶或专题内容在后。没有明显依赖关系时，按名称排序。
- 缺少父级 `index.md` 时主动创建。文章标题或主题变化时，同步更新对应条目的链接、名称和简介；移动或删除文章时，移除旧位置的失效条目并在新位置补充。
- 索引维护只修改受当前文档变更影响的内容，不改写或重排无关领域。

## 代码提交

- 提交代码时，若 `content` 存在未提交更改，可以将这些更改及其受影响的逐级 `index.md` 一并纳入提交。
